# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 3f013167-09cc-4ced-a728-bf66eb96ed35

sync:
  include:
    - "**/*"

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405604641902977.17.azuredatabricks.net
      presets:
      source_linked deployment: false
    variables:
      catalog: spotify_cata
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host:  https://adb-7405604641902977.17.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/nithyakonda5902@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: spotify_cata
      schema: prod
    permissions:
      - user_name: nithyakonda5902@gmail.com
        level: CAN_MANAGE
